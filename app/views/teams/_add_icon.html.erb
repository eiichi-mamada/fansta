<%= form_with(model: team, local: true) do |f| %>
  <%= f.file_field :icon, accept: 'image/jpeg,image/gif,image/png',class: 'm-1'  %>
  <br />
  <%= f.submit 'アイコン編集', class: 'btn btn-info btn-sm m-2' %>
<% end %>