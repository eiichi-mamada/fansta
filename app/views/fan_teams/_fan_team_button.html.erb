<% if user_signed_in? %>　
  <% if team.fan_team?(current_user) %>
    <%= form_with(model: team.fan_teams.find_by(user_id: current_user.id), local: true, method: :delete) do |f| %>
      <%= hidden_field_tag :team_id, team.id %>
      <%= f.submit 'UnFan', class: 'btn btn-danger btn-sm' %>
    <% end %>
  <% else %>
    <%= form_with(model: team.fan_teams.build, local: true) do |f| %>
      <%= hidden_field_tag :team_id, team.id %>
      <%= f.submit 'FanTeam', class: 'btn btn-primary btn-sm' %>
    <% end %>
  <% end %>
<% end %>